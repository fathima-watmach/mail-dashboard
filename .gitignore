# Environment variables — NEVER commit these
backend/.env
.env

# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Frontend build output (generated on Render)
frontend/dist/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Python virtualenv (unrelated to this Node project, left over from a local tool)
.venv/

# Reference documents dropped in the repo root for context during a session —
# not project source, shouldn't be committed
*.pdf
*.xlsx
~$*

# Claude Code personal settings/hooks (not shared with the team)
.claude/settings.local.json
.claude/.agents-md-stop-pending
